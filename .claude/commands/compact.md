---
description: Compact this session using the League Page compaction directions
---

Read `compact.md` at the repository root and summarise this conversation
exactly as it directs.

If `$ARGUMENTS` is non-empty, treat it as an additional emphasis for this
one compaction — it narrows what to keep, it never licenses dropping
anything `compact.md` says to preserve verbatim.

Note: `/compact` is also a Claude Code built-in. If the built-in handles
the invocation instead of this file, the same directions still apply,
because `CLAUDE.md` points every session at `compact.md`.
